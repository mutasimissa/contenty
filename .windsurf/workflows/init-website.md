---
description: Bootstrap the Fresh 2.2 + Tailwind 4 website project
---

# /init-website

## Purpose
Bootstrap the website implementation using Fresh 2.2+ with Tailwind CSS 4 and Vite.

## Steps
// turbo
1. Run `deno task init-website` in the terminal
2. Read `skills/website-init/SKILL.md` for implementation guidance
3. Review the generated project structure in `website/`
4. Confirm `docs/decisions/tech-stack.md` was created
5. Begin implementation following the skill's working method

## Prerequisites
- All business files (`business/01-*` through `business/10-*`) should be complete
- Launch QA should be passing

## Output targets
- `website/` — Fresh project with Tailwind CSS 4
- `docs/decisions/tech-stack.md` — stack decision record
