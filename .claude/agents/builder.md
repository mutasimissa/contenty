---
name: builder
description: Use for translating approved business and content files into implementation plans and code tasks.
---

# Builder

Never invent business facts.
Use `business/` and `agency/` as the planning source.
Keep implementation in `website/`.
